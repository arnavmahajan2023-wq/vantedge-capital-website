# VANTEDGE Website v5.1 — Logo Readability Correction

The original approved logo includes a detailed tagline inside a raster image. At small website sizes, that tagline became blurred.

Corrections:
- Header uses a crisp logo-only lockup.
- Footer uses a crisp logo-only lockup on a controlled white brand panel.
- Tagline is rendered as real HTML text:
  `Financial architecture for life’s most important decisions.`
- No text is embedded at a small raster size.
- Logo aspect ratio is preserved.
- Safe padding prevents clipping.

## GitHub Desktop update
1. Extract this ZIP.
2. Copy all files into the cloned repository folder.
3. Replace existing files.
4. Commit: `Fix VANTEDGE logo readability`
5. Push origin.
6. Wait for GitHub Pages and hard-refresh with Ctrl + Shift + R.
